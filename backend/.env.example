# Copy to .env on the Hostinger server. NEVER commit the real .env.
PORT=5000

# Use the IP if the hostname is not resolvable from your app server
DB_HOST=srv1874.hstgr.io
DB_PORT=3306
DB_USER=u851000947_suryacineprops
DB_PASSWORD=change_me_rotate_this
DB_NAME=u851000947_SuryaCineProps

# Comma-separated list of allowed frontend origins
CORS_ORIGIN=http://localhost:8080,https://your-lovable-domain.lovable.app